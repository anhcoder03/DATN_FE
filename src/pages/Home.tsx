import React from "react";
import { Layout } from "../components/layout";

interface Props {}

const Home = (props: Props) => {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
};

export default Home;
