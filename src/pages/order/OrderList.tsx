import React from "react";
import { Layout } from "../../components/layout";

type Props = {};

const OrderList = (props: Props) => {
  return <Layout>OrderList</Layout>;
};

export default OrderList;
