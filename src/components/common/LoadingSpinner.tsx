const LoadingSpinner = () => {
  return (
    <div className="inline-block w-8 h-8 border-4 border-t-4 rounded-full border-primary border-t-transparent animate-spin"></div>
  );
};

export default LoadingSpinner;
